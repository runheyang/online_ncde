# 临时 eval 配置：对应旧路径 (topk + ncde_align_infos) 训练的 200x200x16 模型。
# data/eval 段继承旧版 eval.yaml，仅覆盖 200x200x16 的 dilation。
base_config: ../../online_ncde/fast_opusv1t__slow_opusv2l/eval.yaml

model:
  func_g_body_dilations: [1, 3, 5]
